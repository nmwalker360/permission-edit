<%= form_for (@new) do |f| %>
 
  <% if @new.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@sublist.errors.count, "error") %> prohibited
        this Testimony from being saved:
      </h2>
      <ul>
        <% @new.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

 
<% end %>